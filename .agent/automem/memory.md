# 🧠 AICONTENT Memory System (AutoMem)

**Status:** ACTIVE 🟢
**Server:** `http://148.251.167.227:8001`
**Model:** Voyage AI (`voyage-3-large`)

---

## 🏗 Как это работает?

Мы используем гибридную систему памяти **AutoMem**, которая объединяет два подхода:
1.  **Vector Store (Qdrant)**: Поиск по смыслу. Позволяет найти "похожие" посты, даже если слова разные.
    *   *Пример:* Запрос "Скандалы Илона Маска" найдет пост про "Grok Nudes".
2.  **Graph Store (FalkorDB)**: Граф знаний. Хранит связи между сущностями.
    *   *Пример:* Пост A -> `RELATES_TO` -> Тема B -> `HAS_PERFORMANCE` -> High Views.

---

## 🔄 Протокол работы агента

Каждый агент **ОБЯЗАН** следовать циклу `Recall -> Act -> Store`.

### 1. Recall (Вспомнить)
**Перед** генерацией контента агент должен спросить память:
> "Что мы знаем о [Тема]?"
> "Какие подходы сработали для [Категория]?"

**Зачем:**
*   Избежать повторов (если мы писали об этом вчера).
*   Сделать отсылку ("Как мы писали ранее...").
*   Использовать успешный формат (если прошлый пост набрал 100к, повтори структуру).

### 2. Act (Действовать)
Агент генерирует контент, используя контекст из памяти.
*   Если память вернула *"Тема Х плохо заходит"*, агент должен изменить угол подачи.

### 3. Store (Запомнить)
**После** генерации агент сохраняет результат.

**Что сохраняем:**
*   **Content:** Сам текст поста (или саммари).
*   **Tags:** `#agent_name`, `#platform`, `#topic`.
*   **Metadata:**
    ```json
    {
      "url": "путь/к/файлу",
      "date": "2026-01-18",
      "type": "post"
    }
    ```

---

## 🛠 Инструментарий

### Python Client
Лежит в `scripts/automem_client.py`. Используется скриптами и агентами.

```python
from scripts.automem_client import AutoMemClient
client = AutoMemClient()

# Вспомнить
context = client.recall("Deep seek AI china")

# Сохранить
client.add_memory("Post text...", tags=["alpha_news"])
```

### Структура Памяти (Schema)

| Тип | Описание | Пример |
|---|---|---|
| **Context** | Основной контент (посты, статьи) | Текст новости про WEF |
| **Concept** | Темы и сущности | "Artificial Intelligence", "Elon Musk" |
| **Insight** | Выводы и стратегии | "Для TikTok нужны хуки 3 сек" |
| **Action** | События | "Опубликовали пост", "Удалили видео" |

---

## 🤖 Интеграция Агентов

### Alpha News 📰
*   **Recall:** Ищет прошлые новости по теме, чтобы не повторяться.
*   **Store:** Сохраняет каждый пост для истории.

### Deep Science 🔬
*   **Recall:** Проверяет научную точность и прошлые разборы.
*   **Store:** Сохраняет сложные концепты, чтобы не объяснять их заново.

### Eden AI 🎬
*   **Recall:** Ищет вирусные хуки и успешные триггеры.
*   **Store:** Сохраняет сценарии и статистику (Views).
